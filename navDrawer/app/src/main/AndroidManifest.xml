<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.navdrawer">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Principal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.navdrawer.PosiblesSintomas"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name="com.example.navdrawer.Sintomas"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="com.example.navdrawer.ui.Menu"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="com.example.navdrawer.ui.Recomendaciones"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="com.example.navdrawer.ControlRegistrar"
            android:theme="@style/AppTheme">
        </activity>

    </application>

    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="@string/google_maps_key" />

</manifest>